<!DOCTYPE html>
<html lang="pt-BR" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RDP STUDIO - Projetos</title>
    <link rel="apple-touch-icon" sizes="180x180" href="../apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="../favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="../favicon-16x16.png">
    <link rel="manifest" href="../site.webmanifest">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=JetBrains+Mono:wght@400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        deepBlue: '#1E3A5F',
                        techGray: '#2D3748',
                        vibrantCyan: '#00B4D8',
                        energeticAmber: '#F59E0B',
                        trustGreen: '#10B981',
                        darkSlate: '#020617',
                    },
                    fontFamily: {
                        sans: ['Inter', 'sans-serif'],
                        mono: ['JetBrains Mono', 'monospace'],
                    },
                    animation: {
                        'fade-in-up': 'fadeInUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards',
                    }
                }
            }
        }
    </script>
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #020617;
            background-image:
                radial-gradient(circle at 15% 50%, rgba(30, 58, 95, 0.15), transparent 25%),
                radial-gradient(circle at 85% 30%, rgba(0, 180, 216, 0.05), transparent 40%);
            background-attachment: fixed;
            color: #F8FAFC;
        }

        /* --- SPOTLIGHT EFFECT --- */
        .spotlight-card {
            position: relative;
            background: rgba(30, 41, 59, 0.3);
            border-radius: 1rem;
            overflow: hidden;
            border: 1px solid rgba(255, 255, 255, 0.05);
            transition: transform 0.3s ease, opacity 0.4s ease;
        }

        /* The glow layer */
        .spotlight-card::before {
            content: "";
            position: absolute;
            height: 100%;
            width: 100%;
            top: 0;
            left: 0;
            background: radial-gradient(
                800px circle at var(--mouse-x) var(--mouse-y),
                rgba(0, 180, 216, 0.15),
                transparent 40%
            );
            opacity: 0;
            transition: opacity 0.5s;
            pointer-events: none;
            z-index: 1;
        }

        /* The border glow layer */
        .spotlight-card::after {
            content: "";
            position: absolute;
            height: 100%;
            width: 100%;
            top: 0;
            left: 0;
            background: radial-gradient(
                600px circle at var(--mouse-x) var(--mouse-y),
                rgba(0, 180, 216, 0.4),
                transparent 40%
            );
            opacity: 0;
            z-index: 2;
            pointer-events: none;
            transition: opacity 0.5s;
            /* This is the magic for border only */
            mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
            -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
            mask-composite: exclude;
            -webkit-mask-composite: xor;
            padding: 1px; /* Border width */
            border-radius: inherit;
        }

        .spotlight-card:hover::before,
        .spotlight-card:hover::after {
            opacity: 1;
        }

        /* --- FILTER ANIMATION --- */
        .filter-btn {
            transition: all 0.3s ease;
        }
        .filter-btn.active {
            background-color: rgba(0, 180, 216, 0.1);
            color: #00B4D8;
            border-color: rgba(0, 180, 216, 0.3);
        }

        /* --- UTILS --- */
        .nav-link { color: #94a3b8; transition: color 0.3s; }
        .nav-link:hover, .nav-link.active { color: #fff; }

        @keyframes fadeInUp { to { opacity: 1; transform: translateY(0); } }
        .fade-in-up { opacity: 0; transform: translateY(20px); animation: fadeInUp 0.8s forwards; }

        .watermark-container::after {
            content: "© 2026 RDP STUDIO. Desenvolvido por Marcelo Rodrigues.";
            font-size: 0.75rem; color: #64748b; opacity: 0.5;
            display: block; pointer-events: none;
        }

        /* Bento Grid Specifics */
        .bento-grid {
            display: grid;
            grid-template-columns: repeat(1, 1fr);
            gap: 1.5rem;
            auto-rows: minmax(200px, auto);
        }
        @media (min-width: 768px) {
            .bento-grid {
                grid-template-columns: repeat(2, 1fr);
            }
        }
        @media (min-width: 1024px) {
            .bento-grid {
                grid-template-columns: repeat(3, 1fr);
            }
            .col-span-2 { grid-column: span 2; }
            .row-span-2 { grid-row: span 2; }
        }

    </style>
</head>
<body class="antialiased selection:bg-vibrantCyan selection:text-white" onmousemove="handleMouseMove(event)">

    <div class="max-w-7xl mx-auto px-6 py-6 min-h-screen flex flex-col relative">

        <!-- Header -->
        <header class="flex justify-between items-center mb-16 relative z-50">
            <a href="index.html" class="flex items-center gap-4 group">
                <div class="w-10 h-10 rounded-xl bg-white/5 border border-white/10 flex items-center justify-center p-1 backdrop-blur-md">
                     <img src="../assets/images/branding/logo.png" alt="Logo" class="w-full h-full object-contain group-hover:scale-110 transition-transform">
                </div>
                <h1 class="text-xl font-bold tracking-tight text-white">RDP <span class="text-vibrantCyan font-light">STUDIO</span></h1>
            </a>

            <!-- Nav (Desktop) -->
            <nav class="hidden md:flex items-center gap-8 text-sm font-medium">
                <a href="index.html" class="nav-link">Home</a>
                <a href="projetos.html" class="nav-link active text-vibrantCyan">Projetos</a>
                <a href="index.html#empresa" class="nav-link">A Empresa</a>
                <a href="sobre.html" class="nav-link">Sobre Mim</a>
                <a href="https://wa.me/5562996780498" target="_blank" class="px-4 py-2 rounded-lg bg-vibrantCyan/10 text-vibrantCyan hover:bg-vibrantCyan hover:text-white transition-all border border-vibrantCyan/20">Contato</a>
            </nav>

            <!-- Mobile Menu Button -->
            <button id="mobile-menu-btn" class="md:hidden text-white text-2xl p-2 z-50 relative group">
                <div class="absolute inset-0 bg-white/5 rounded-lg opacity-0 group-hover:opacity-100 transition-opacity"></div>
                <i class="fa-solid fa-bars relative z-10"></i>
            </button>
        </header>

        <!-- Mobile Menu Overlay -->
        <div id="mobile-menu-overlay" class="fixed inset-0 z-40 bg-slate-950/98 backdrop-blur-xl transform translate-x-full transition-transform duration-300 flex flex-col items-center justify-center space-y-8 md:hidden">
            <a href="index.html" class="text-2xl font-bold text-white hover:text-vibrantCyan transition-colors mobile-link">Home</a>
            <a href="projetos.html" class="text-2xl font-bold text-vibrantCyan hover:text-vibrantCyan transition-colors mobile-link">Projetos</a>
            <a href="index.html#empresa" class="text-2xl font-bold text-white hover:text-vibrantCyan transition-colors mobile-link">A Empresa</a>
            <a href="sobre.html" class="text-2xl font-bold text-white hover:text-vibrantCyan transition-colors mobile-link">Sobre Mim</a>
            <a href="https://wa.me/5562996780498" target="_blank" class="px-8 py-3 rounded-full bg-vibrantCyan text-white font-bold text-lg shadow-lg shadow-vibrantCyan/20 mobile-link">Contato</a>
        </div>

        <!-- Main Content -->
        <main class="flex-grow">

            <!-- Page Header -->
            <div class="flex flex-col md:flex-row md:items-end justify-between gap-6 mb-12 fade-in-up">
                <div>
                    <h2 class="text-4xl md:text-5xl font-extrabold text-white tracking-tight mb-2">
                        Engenharia <span class="text-transparent bg-clip-text bg-gradient-to-r from-vibrantCyan to-trustGreen">Digital</span>
                    </h2>
                    <p class="text-slate-400 font-light max-w-xl">
                        Explorando a interseção entre infraestrutura crítica, segurança ofensiva e inteligência artificial.
                    </p>
                </div>

                <!-- Filters -->
                <div class="flex flex-wrap gap-2" id="filter-container">
                    <button class="filter-btn active px-4 py-2 rounded-full border border-white/5 text-xs font-mono text-slate-400 hover:text-white hover:border-white/20 bg-slate-900/50 backdrop-blur" data-filter="all">ALL</button>
                    <button class="filter-btn px-4 py-2 rounded-full border border-white/5 text-xs font-mono text-slate-400 hover:text-white hover:border-white/20 bg-slate-900/50 backdrop-blur" data-filter="intelligence">INTELLIGENCE</button>
                    <button class="filter-btn px-4 py-2 rounded-full border border-white/5 text-xs font-mono text-slate-400 hover:text-white hover:border-white/20 bg-slate-900/50 backdrop-blur" data-filter="automation">AI / AUTOMATION</button>
                    <button class="filter-btn px-4 py-2 rounded-full border border-white/5 text-xs font-mono text-slate-400 hover:text-white hover:border-white/20 bg-slate-900/50 backdrop-blur" data-filter="security">SECURITY</button>
                </div>
            </div>

            <!-- BENTO GRID -->
            <div class="bento-grid fade-in-up" style="animation-delay: 0.15s" id="projects-grid">

                <!-- 1. RDP INSIDER (Large / Featured) -->
                <div class="spotlight-card group col-span-1 lg:col-span-2 relative flex flex-col md:flex-row overflow-hidden" data-category="intelligence">
                    <!-- Decor: Radar Scan -->
                    <div class="absolute top-0 right-0 w-64 h-64 bg-pink-500/10 rounded-full blur-3xl -translate-y-1/2 translate-x-1/2 pointer-events-none"></div>

                    <div class="p-8 flex flex-col justify-between w-full md:w-2/3 z-10">
                        <div>
                            <div class="flex items-center gap-3 mb-4">
                                <div class="w-8 h-8 rounded bg-pink-500/10 flex items-center justify-center text-pink-400 border border-pink-500/20">
                                    <i class="fa-solid fa-radar"></i>
                                </div>
                                <span class="text-xs font-mono text-pink-400 tracking-wider">INTELLIGENCE_OPS</span>
                            </div>
                            <h3 class="text-2xl font-bold text-white mb-3 group-hover:text-vibrantCyan transition-colors">RDP Insider</h3>
                            <p class="text-slate-400 text-sm leading-relaxed mb-6">
                                Plataforma de inteligência Geek e monitoramento de vazamentos. Utiliza crawlers Python distribuídos para agregar dados da Epic Games, RSS Feeds e fontes de hardware em tempo real. Inclui sistema de detecção de "Mystery Games".
                            </p>
                        </div>
                        <div class="flex gap-3 mt-auto">
                            <a href="../projects/scanner-game-free/index.html" class="inline-flex items-center gap-2 px-5 py-2.5 rounded-lg bg-white/5 border border-white/10 hover:bg-pink-500/10 hover:border-pink-500/30 hover:text-pink-400 text-slate-300 text-sm font-medium transition-all">
                                <i class="fa-solid fa-terminal"></i> Acessar Terminal
                            </a>
                            <a href="https://github.com/MarceloRDPJ" target="_blank" class="inline-flex items-center justify-center w-10 h-10 rounded-lg bg-white/5 border border-white/10 hover:bg-white/10 text-slate-400 transition-all">
                                <i class="fa-brands fa-github"></i>
                            </a>
                        </div>
                    </div>

                    <!-- Visual Side -->
                    <div class="hidden md:flex w-1/3 bg-slate-900/50 border-l border-white/5 items-center justify-center relative overflow-hidden">
                        <div class="absolute inset-0 bg-[url('data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMjAiIGhlaWdodD0iMjAiIHhtbG5zPSJodHRwOi8vd3d3LnczLm9yZy8yMDAwL3N2ZyI+PGNpcmNsZSBjeD0iMiIgY3k9IjIiIHI9IjEiIGZpbGw9InJnYmEoMjU1LDI1NSwyNTUsMC4wNSkiLz48L3N2Zz4=')] opacity-50"></div>
                        <div class="text-center relative z-10">
                            <div class="text-4xl font-mono font-bold text-white mb-1">4.2</div>
                            <div class="text-[10px] text-pink-400 uppercase tracking-widest">Version</div>
                        </div>
                    </div>
                </div>

                <!-- 2. TECH STACK (Mini Card) -->
                <div class="spotlight-card p-6 flex flex-col justify-center items-center text-center bg-gradient-to-br from-slate-900 to-slate-800" data-category="all">
                    <div class="w-12 h-12 rounded-full bg-vibrantCyan/10 flex items-center justify-center text-vibrantCyan mb-4 border border-vibrantCyan/20 shadow-[0_0_15px_rgba(0,180,216,0.2)]">
                        <i class="fa-brands fa-python text-xl"></i>
                    </div>
                    <div class="text-2xl font-bold text-white">15k+</div>
                    <div class="text-xs text-slate-500 font-mono uppercase mt-1">Lines of Code</div>
                </div>

                <!-- 3. AUTOMAÇÃO GLPI (Standard) -->
                <div class="spotlight-card group p-8 flex flex-col relative" data-category="automation">
                    <div class="absolute top-0 left-0 w-32 h-32 bg-indigo-500/10 rounded-full blur-3xl -translate-x-1/2 -translate-y-1/2 pointer-events-none"></div>
                    <div class="flex justify-between items-start mb-6">
                        <div class="w-10 h-10 rounded bg-indigo-500/10 flex items-center justify-center text-indigo-400 border border-indigo-500/20">
                            <i class="fa-solid fa-server"></i>
                        </div>
                        <span class="px-2 py-1 rounded text-[10px] font-bold bg-indigo-500/10 text-indigo-400 border border-indigo-500/20">INFRA</span>
                    </div>
                    <h3 class="text-xl font-bold text-white mb-2 group-hover:text-vibrantCyan transition-colors">Automação GLPI</h3>
                    <p class="text-sm text-slate-400 mb-6 flex-grow">
                        Orquestração de chamados massivos. Script Python que varre diretórios locais e interage via API para upload de evidências e registro automático.
                    </p>
                    <a href="../projects/abertura-chamados-glpi/index.html" class="flex items-center gap-2 text-sm font-bold text-slate-300 hover:text-indigo-400 transition-colors">
                        Ver Documentação <i class="fa-solid fa-arrow-right text-xs"></i>
                    </a>
                </div>

                <!-- 4. BOT DE GESTÃO (Standard) -->
                <div class="spotlight-card group p-8 flex flex-col relative" data-category="automation">
                    <div class="absolute bottom-0 right-0 w-32 h-32 bg-purple-500/10 rounded-full blur-3xl translate-x-1/2 translate-y-1/2 pointer-events-none"></div>
                    <div class="flex justify-between items-start mb-6">
                        <div class="w-10 h-10 rounded bg-purple-500/10 flex items-center justify-center text-purple-400 border border-purple-500/20">
                            <i class="fa-solid fa-robot"></i>
                        </div>
                        <span class="px-2 py-1 rounded text-[10px] font-bold bg-purple-500/10 text-purple-400 border border-purple-500/20">AI/BOT</span>
                    </div>
                    <h3 class="text-xl font-bold text-white mb-2 group-hover:text-vibrantCyan transition-colors">Bot Lume Beauty</h3>
                    <p class="text-sm text-slate-400 mb-6 flex-grow">
                        Assistente virtual integrado ao Telegram. Gestão de estoque e vendas com OCR (Gemini Pro) para leitura automatizada de comprovantes bancários.
                    </p>
                    <a href="../projects/assistente-vendas-ia/index.html" class="flex items-center gap-2 text-sm font-bold text-slate-300 hover:text-purple-400 transition-colors">
                        Testar Simulador <i class="fa-solid fa-arrow-right text-xs"></i>
                    </a>
                </div>

                <!-- 5. VALIDADOR FIREWALL (Standard) -->
                <div class="spotlight-card group p-8 flex flex-col relative" data-category="security">
                    <div class="absolute top-0 right-0 w-32 h-32 bg-energeticAmber/10 rounded-full blur-3xl translate-x-1/2 -translate-y-1/2 pointer-events-none"></div>
                    <div class="flex justify-between items-start mb-6">
                        <div class="w-10 h-10 rounded bg-energeticAmber/10 flex items-center justify-center text-energeticAmber border border-energeticAmber/20">
                            <i class="fa-solid fa-shield-cat"></i>
                        </div>
                        <span class="px-2 py-1 rounded text-[10px] font-bold bg-energeticAmber/10 text-energeticAmber border border-energeticAmber/20">SEC</span>
                    </div>
                    <h3 class="text-xl font-bold text-white mb-2 group-hover:text-vibrantCyan transition-colors">Validador MAC</h3>
                    <p class="text-sm text-slate-400 mb-6 flex-grow">
                        Ferramenta de sanitização de dados para Fortigate. Garante integridade de objetos de rede e evita duplicidade em migrações críticas.
                    </p>
                    <a href="../projects/validador-firewall/index.html" class="flex items-center gap-2 text-sm font-bold text-slate-300 hover:text-energeticAmber transition-colors">
                        Iniciar Tool <i class="fa-solid fa-arrow-right text-xs"></i>
                    </a>
                </div>

                <!-- 6. SECURITY STATS (Mini Card) -->
                <div class="spotlight-card p-6 flex flex-col justify-center items-center text-center bg-gradient-to-tl from-slate-900 to-slate-800" data-category="all">
                    <div class="w-12 h-12 rounded-full bg-trustGreen/10 flex items-center justify-center text-trustGreen mb-4 border border-trustGreen/20 shadow-[0_0_15px_rgba(16,185,129,0.2)]">
                        <i class="fa-solid fa-lock text-xl"></i>
                    </div>
                    <div class="text-2xl font-bold text-white">100%</div>
                    <div class="text-xs text-slate-500 font-mono uppercase mt-1">Secure Deploy</div>
                </div>

            </div>

        </main>

        <!-- Footer -->
        <footer class="border-t border-slate-800/50 pt-12 pb-8 mt-auto">
            <div class="flex flex-col items-center text-center">
                <div class="flex justify-center gap-8 mb-8">
                    <a href="https://instagram.com/marcelo_rpj" target="_blank" aria-label="Instagram" class="text-slate-400 hover:text-vibrantCyan transition-colors text-2xl transform hover:scale-110 duration-200">
                        <i class="fa-brands fa-instagram"></i>
                    </a>
                    <a href="https://www.linkedin.com/in/marcelo-rodrigues-088478211/" target="_blank" aria-label="LinkedIn" class="text-slate-400 hover:text-vibrantCyan transition-colors text-2xl transform hover:scale-110 duration-200">
                        <i class="fa-brands fa-linkedin"></i>
                    </a>
                    <a href="mailto:rdpstudio@gmail.com" aria-label="Email" class="text-slate-400 hover:text-vibrantCyan transition-colors text-2xl transform hover:scale-110 duration-200">
                        <i class="fa-regular fa-envelope"></i>
                    </a>
                </div>
                <p class="text-sm text-slate-500 mb-2">
                    © 2026 RDP STUDIO. Todos os direitos reservados.
                </p>
                <p class="text-xs text-slate-600 font-mono">
                    Desenvolvido com <i class="fa-solid fa-heart text-red-900 animate-pulse"></i> por Marcelo Rodrigues
                </p>
                <div class="watermark-container hidden"></div>
            </div>
        </footer>

    </div>

    <!-- Scripts -->
    <script>
        // --- SPOTLIGHT EFFECT LOGIC ---
        function handleMouseMove(e) {
            const cards = document.querySelectorAll(".spotlight-card");
            for(const card of cards) {
                const rect = card.getBoundingClientRect();
                const x = e.clientX - rect.left;
                const y = e.clientY - rect.top;

                card.style.setProperty("--mouse-x", `${x}px`);
                card.style.setProperty("--mouse-y", `${y}px`);
            }
        }

        // --- FILTER LOGIC ---
        const filterBtns = document.querySelectorAll('.filter-btn');
        const projectItems = document.querySelectorAll('.spotlight-card');

        filterBtns.forEach(btn => {
            btn.addEventListener('click', () => {
                // Active state
                filterBtns.forEach(b => b.classList.remove('active'));
                btn.classList.add('active');

                const filter = btn.getAttribute('data-filter');

                projectItems.forEach(item => {
                    const category = item.getAttribute('data-category');

                    if (filter === 'all' || category === filter || category === 'all') { // 'all' category items show always? No, wait. 'all' filter shows everything. 'all' category items (stats) should show on all filters? Let's say yes for aesthetics.
                         // Logic: If filter is ALL, show everything.
                         // If filter is specific, show matching category AND items marked as 'all' (stats) to keep layout balanced?
                         // Let's keep it simple: Show matching.
                         if (filter === 'all' || category === filter) {
                             item.style.display = 'flex';
                             setTimeout(() => {
                                 item.style.opacity = '1';
                                 item.style.transform = 'translateY(0) scale(1)';
                             }, 50);
                         } else {
                             item.style.opacity = '0';
                             item.style.transform = 'translateY(10px) scale(0.95)';
                             setTimeout(() => {
                                 item.style.display = 'none';
                             }, 300);
                         }
                    } else {
                         item.style.opacity = '0';
                         item.style.transform = 'translateY(10px) scale(0.95)';
                         setTimeout(() => {
                             item.style.display = 'none';
                         }, 300);
                    }
                });
            });
        });

        // --- MOBILE MENU LOGIC ---
        const menuBtn = document.getElementById('mobile-menu-btn');
        const menuOverlay = document.getElementById('mobile-menu-overlay');
        const mobileLinks = document.querySelectorAll('.mobile-link');
        let isMenuOpen = false;

        if (menuBtn && menuOverlay) {
            menuBtn.addEventListener('click', () => {
                isMenuOpen = !isMenuOpen;
                const icon = menuBtn.querySelector('i');
                if (isMenuOpen) {
                    menuOverlay.classList.remove('translate-x-full');
                    icon.classList.remove('fa-bars');
                    icon.classList.add('fa-xmark');
                    document.body.style.overflow = 'hidden';
                } else {
                    menuOverlay.classList.add('translate-x-full');
                    icon.classList.remove('fa-xmark');
                    icon.classList.add('fa-bars');
                    document.body.style.overflow = 'auto';
                }
            });

            mobileLinks.forEach(link => {
                link.addEventListener('click', () => {
                    isMenuOpen = false;
                    menuOverlay.classList.add('translate-x-full');
                    const icon = menuBtn.querySelector('i');
                    icon.classList.remove('fa-xmark');
                    icon.classList.add('fa-bars');
                    document.body.style.overflow = 'auto';
                });
            });
        }
    </script>
</body>
</html>
